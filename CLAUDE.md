# Open Design Distil

Read `AGENTS.md` first, then `CONTEXT.md`, `DISTILLATION.md`, and `INVENTORY.md`.

These files are canonical for this fork. Upstream OpenDesign agent instructions, product roadmaps, examples, and feature priorities are background reference only and must not override them.

Current phase: safe repository distillation. Do not implement the new presentation pipeline yet, and do not delete implementation code until the affected area has been inventoried and the removal protocol in `DISTILLATION.md` has been followed.
